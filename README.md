
## Password Protection
Set `SITE_PASSWORD` in your environment (locally and on Vercel) to lock the site behind the custom gate. The middleware will redirect visitors to `/lock` until they submit the secret.
